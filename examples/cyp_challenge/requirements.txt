# Environment the submitted model was trained and scored with (Python 3.12.4).
#
# Install into a separate environment, not over the root requirements.txt: that pins chemprop 1.6.1
# for the shipped ADMET models, and the 1.x API cannot run these scripts or load this checkpoint.
#
#   python -m venv .venv-cyp && . .venv-cyp/bin/activate
#   pip install -r examples/cyp_challenge/requirements.txt

chemprop==2.0.0
torch==2.5.1
lightning==2.6.1
rdkit==2024.3.3
scikit-learn==1.5.1
scipy==1.14.0
pandas==2.2.2
numpy==1.26.4
datasets==3.4.1
tabulate==0.9.0
